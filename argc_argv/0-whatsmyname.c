#include <stdio.h>

/**
 * main - print is name
 * @argc: length of array
 * @argv: vector of array
 * Return: 0
 */

int main( __attribute__((unused)) int argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}

